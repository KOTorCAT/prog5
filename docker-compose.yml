version: '3.8'

services:
  glossary-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./glossary.db
    volumes:
      - glossary_/app
    restart: unless-stopped

volumes:
  glossary_